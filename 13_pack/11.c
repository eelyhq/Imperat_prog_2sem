#include <stdio.h>
#include <stdlib.h>

int main()
{
    FILE* f_in = fopen("input.txt", "r");
    FILE* f_out = fopen("output.txt", "w");

    int cnt_slash = 0;

    while( (c = fgetc(f_in)) != '\n')
    {

    }
}